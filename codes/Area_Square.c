#include<stdio.h>

int main() {
    float a,b;
    printf("Enter the value of Length = ");
    scanf("%f",&a);
    printf("Enter the value of Breadth = ");
    scanf("%f",&b);
    printf("Area = %f\n", a * b);
    return 0;
}